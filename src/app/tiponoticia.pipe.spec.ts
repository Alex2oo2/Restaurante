import { TiponoticiaPipe } from './tiponoticia.pipe';

describe('TiponoticiaPipe', () => {
  it('create an instance', () => {
    const pipe = new TiponoticiaPipe();
    expect(pipe).toBeTruthy();
  });
});
